void downloadFileImpl(List<int> bytes, String fileName) {
  // Compile-safe fallback for mobile/desktop. 
  // Avoids adding extra packages like path_provider to keep web build lightweight.
}

void viewFileImpl(List<int> bytes, String fileName) {
  // Compile-safe fallback for mobile/desktop. 
}
